# Global npm installs go to ~/.local (bin in ~/.local/bin, packages in
# ~/.local/lib/node_modules) instead of next to the node binary. With node from
# Nix the default prefix is the read-only /nix/store, so `npm install -g` and
# `codex update` fail with EACCES; with system node it would need root.
prefix=${HOME}/.local
