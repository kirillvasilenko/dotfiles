return {
  "akinsho/bufferline.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  version = "*",
  opts = {
    options = {
      mode = "tabs",
      -- does not show what tab is active with the default colorscheme
      -- separator_style = "slant",
    },
  },
}